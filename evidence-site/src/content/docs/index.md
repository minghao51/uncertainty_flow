---
title: Evidence portal
description: Verified benchmark runs and metrics.
---

This portal is generated from verified Platinum artifacts.

The exporter writes a small `index.json` plus partitioned `runs/*.jsonl.gz` records. Historical prediction-level Parquet files remain in the artifact store and are not copied into this site.
